/**
 * Manual QA entry points, not part of the shipped product: one {@code main()}
 * per screen/shell, wired to fake data, runnable directly from the IDE so a
 * page or the whole shell can be eyeballed without the real network/auth/
 * game stack running. Never referenced by {@code Main} or bundled as the
 * jar's entry point.
 */
package mc.snakenest.launcher.devpreview;
