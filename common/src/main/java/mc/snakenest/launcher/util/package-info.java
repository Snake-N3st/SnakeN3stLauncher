/**
 * Small, dependency-free utilities shared by every other package: where the
 * launcher's data lives on disk ({@link mc.snakenest.launcher.util.AppDirs}),
 * hex/SHA-256 codecs, atomic file writes, and logging. Nothing in this
 * package depends on any other launcher package.
 */
package mc.snakenest.launcher.util;
